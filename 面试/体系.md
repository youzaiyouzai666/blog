# javascript 基础深入

## [执行上下文&作用域&变量对象](https://juejin.cn/column/7035531575974592520)

```
console.log(foo);// 会打印函数，而不是 undefined 。

function foo(){
    console.log("foo");
}

var foo = 1;
console.log(foo);
```

这是因为在进入执行上下文时，首先会处理函数声明，其次会处理变量声明，如果变量名称跟已经声明的形式参数或函数相同，则变量声明不会干扰已经存在的这类属性。

### 词法作用域-静态作用域

```
var value = 1;

function foo() {
    console.log(value);
}

function bar() {
    var value = 2;
    foo();
}

bar();

```

### this

```
var foo = {
  value: 2,
  bar: function () {
    return this.value;
  }
}

//示例1
console.log(foo.bar());
//示例2
console.log((foo.bar)());
//示例3
console.log((foo.bar = foo.bar)());
//示例4
console.log((false || foo.bar)());
//示例5
console.log((foo.bar, foo.bar)());
```

### [闭包](https://github.com/mqyqingfeng/Blog/issues/9)

```
var data = [];

for (var i = 0; i < 3; i++) {
  data[i] = function () {
    console.log(i);
  };
}

data[0]();
data[1]();
data[2]();
```

```
var data = [];

for (var i = 0; i < 3; i++) {
  data[i] = (function (i) {
        return function(){
            console.log(i);
        }
  })(i);
}

data[0]();
data[1]();
data[2]();
```

## V8 引擎

### [垃圾回收](https://juejin.cn/post/6844904182512615432)

**代际假说**（The Generational Hypothesis）是垃圾回收领域中的一个重要术语，V8 的垃圾回收的策略也是建立在该假说的基础之上。
代际假说也很简单，主要有两个特点：

- 大部分对象在内存中存在的时间很短，简单来说，就是很多对象一经分配内存，很快就变得不可访问。
- 不死的对象，会活的更久。

基于这个这个假说 V8 才会把堆分为新生代和老生代两个区域，同时设计了两个垃圾回收器：

- 副垃圾回收器 负责新生代区域的垃圾回收 - **置换**
- 主垃圾回收器 负责老生代区域的垃圾回收 - **标记-清除**、 **标记-整理**

### 微任务 & 宏任务

[消息队列](https://juejin.cn/post/6844904200199995406)

# react

## react 渲染机制

react 是重运行时的框架(对比 Svelte，重编译的框架，进过编译和预处理，代码基本全部会解析成原生的 DOM 操作)
运行时 主要挑战 CPU 的问题，IO 问题
【CPU】问题：如果 JS 执行超过 16.6ms，那么本次刷新就没有时间执行样式布局和样式绘制了，于是在页面上就会表现为卡顿。而 React 又是一个重运行时，所以需要解决 CPU 的问题。具体解法：实现一种具备任务分割、异步执行、而且还能让出执行权的解决方案呢 [参考](https://zhuanlan.zhihu.com/p/441815358)

- 任务分割
  Fiber 树
